import { RegistrationResponse } from './RegistrationResponse';

describe('RegistrationResponse', () => {
  it('should create an instance', () => {
    expect(new RegistrationResponse()).toBeTruthy();
  });
});
