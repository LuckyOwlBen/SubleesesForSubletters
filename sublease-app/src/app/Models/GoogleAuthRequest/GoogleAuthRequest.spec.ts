import { GoogleAuthRequest } from './GoogleAuthRequest';

describe('RegistrationRequest', () => {
  it('should create an instance', () => {
    expect(new GoogleAuthRequest()).toBeTruthy();
  });
});
